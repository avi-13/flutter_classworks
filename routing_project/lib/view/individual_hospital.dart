import 'package:flutter/material.dart';

class IndividualHospital extends StatelessWidget {
  const IndividualHospital({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
